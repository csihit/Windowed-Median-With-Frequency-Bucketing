# Windowed-Median-With-Frequency-Bucketing
Implements sliding window median using frequency bucketing (counting array) to avoid sorting each window. Maintains element frequencies for efficient updates and computes median via cumulative counts. Optimized for fixed value ranges, enabling faster window transitions and real-time median calculation.
